---
hazelcast:
    network:
        join:
            multicast:
                enabled: false
            kubernetes:
                enabled: false
                namespace: novo-dev
                service-name: api-microservices-k8s-svc
